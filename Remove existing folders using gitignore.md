1. remove existing unwanted folder

git rm --cached -r codesnippets/

2. commit after removing the folder

git commit -m "update"  

3. push after commiting the removed folder

git push -u origin main